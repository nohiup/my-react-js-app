const classes = {
  //Const classes
  cardFullHeight: "flex flex-col h-full",
  itemCentralize: "flex items-center justify-center",
  textNormalDescription: "text-xs text-normal",
  textContent: "text-sm text-normal",
  //Functions
  squareContainer: (size: number) => `w-${size} h-${size}`,
}
export default classes;